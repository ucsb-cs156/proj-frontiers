import BasicLayout from "main/layouts/BasicLayout/BasicLayout";

export default function NoPermsLinkPage() {
    return <BasicLayout>
        <h2>You do not have the permissions to link this course to an organization on GitHub.</h2>
    </BasicLayout>
}