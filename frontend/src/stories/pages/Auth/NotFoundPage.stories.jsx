import React from "react";
import NotFoundPage from "main/pages/Auth/NotFoundPage";

export default {
  title: "pages/Auth/NotFoundPage",
  component: NotFoundPage,
};

const Template = () => <NotFoundPage />;

export const Default = Template.bind({});
